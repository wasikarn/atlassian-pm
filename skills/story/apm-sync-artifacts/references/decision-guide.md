# Decision Guide

## When to Use

> `/sync-artifacts` handles both Jira-only cascades (Story → Sub-tasks) and full artifact sync (+ Confluence).
> If only Jira sub-tasks need updating, it skips Confluence phases automatically.
